# TASK-260213-1g3gvv: scaffold-go-project

## Description
Initialize Go module, set up project structure per agent-facing-api architecture: cmd/ (root, query, grep, calc), internal/ (domain, fields, query, search, calculators/). Add cobra dependency. Create Makefile for build/test/install. Symlink binary to ~/.local/bin/appraise.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
