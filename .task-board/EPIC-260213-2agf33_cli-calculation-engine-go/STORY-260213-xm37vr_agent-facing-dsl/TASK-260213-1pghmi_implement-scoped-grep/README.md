# TASK-260213-1pghmi: implement-scoped-grep

## Description
Go package: internal/search/grep.go. Implement scoped regex search across evaluation data files. 'appraise grep <pattern>' searches JSON data files in the working directory. Support --file filter, -i case-insensitive, -C context lines. Follow agent-facing-api assets/scoped-grep.go.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
