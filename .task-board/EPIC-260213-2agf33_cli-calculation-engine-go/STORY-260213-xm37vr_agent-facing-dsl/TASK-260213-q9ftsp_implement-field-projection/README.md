# TASK-260213-q9ftsp: implement-field-projection

## Description
Go package: internal/fields/selector.go. Implement field projection for calculation results. Define presets: 'minimal' (just the result value), 'default' (value + inputs), 'full' (value + inputs + benchmarks + diagnosis). Support explicit field lists: { bvr tierGaps diagnosis }. Apply projection to any calculator output struct. Follow agent-facing-api assets/field-selector.go.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
