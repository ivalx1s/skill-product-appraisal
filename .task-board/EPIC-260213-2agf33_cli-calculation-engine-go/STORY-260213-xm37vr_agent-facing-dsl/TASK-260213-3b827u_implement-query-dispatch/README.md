# TASK-260213-3b827u: implement-query-dispatch

## Description
Go package: internal/query/ops.go. Implement query dispatch: route parsed DSL operations to calculator functions. Operations: calc(pricing.bvr, ...), calc(bundle.deadweight, ...), calc(scoring.gonogo, ...), schema(), batch(). Support --format json|compact. Wire up cmd/query.go cobra command. Support reading input data from --input flag (JSON file path) or stdin.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
