# TASK-260213-200tp6: package-cli-binary

## Description
Add build/install to Makefile. Add CLI binary to product-appraisal/scripts/ or make it installable via 'go install'. Create symlink to ~/.local/bin/appraise. Verify agent can call 'appraise q schema()' and get response.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
